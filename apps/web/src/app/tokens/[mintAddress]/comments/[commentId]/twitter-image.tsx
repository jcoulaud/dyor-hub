// Use the same image generator for Twitter as for OpenGraph
export { contentType, default, size } from './opengraph-image';

// Specific Twitter alt text
export const alt = 'DYOR hub - Comment shared on Twitter';
